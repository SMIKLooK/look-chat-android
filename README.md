# Look Chat — Android-клиент для look-backend

Мини-чат и голосовой ассистент на Kotlin (Jetpack Compose, Material 3).
Диалог ведётся с памятью: история переписки хранится на бекенде в рамках
сессии и отправляется модели вместе с каждым новым запросом.
Пишете вопрос текстом или говорите голосом — приложение отправляет его
на look-backend (`POST /api/v1/chat`) и показывает ответ модели прямо
в чате, голосом или текстом.
Ошибки бекенда выводятся как сообщения с красным фоном, у ответов модели
подпись: имя модели, провайдер и время обработки.

Скриншоты (сняты на эмуляторе с работающим бекендом) — в папке
[screenshots/](screenshots/): запуск, ответ echo, реальный ответ
gemini-3.6-flash, ошибка KEYWORD_NOT_FOUND.

## Голосовой ассистент

Кнопка 🎙 в шапке включает ассистента — дальше можно не трогать телефон:

- работает **в фоне** через foreground-сервис с постоянным уведомлением
  («Look Ассистент — Слушаю ключевое слово „лук"»); приложение можно
  свернуть, сервис продолжит слушать;
- непрерывно распознаёт речь (системный `SpeechRecognizer`, ru-RU) и ждёт
  слово **«лук»** — фразы без него игнорируются;
- «лук гемини расскажи анекдот» → на бекенд уходит
  «лук gemini расскажи анекдот» (русские названия моделей нормализуются:
  гемини/джемини → gemini, клод → claude, эхо → echo);
- если модель не назвали («лук какая погода») — подставляется gemini;
- **ответ приходит текстом в чат и озвучивается голосом** (системный TTS,
  русский голос); пока ассистент говорит, микрофон приостановлен;
- **тап по строке статуса («🔊 Отвечаю голосом…») останавливает озвучку**;
- **кодовое слово настраивается** в настройках (⚙): по умолчанию «лук»,
  можно любое своё (например «джарвис»). Оно нужно только для старта
  записи — на бекенд не отправляется, там формат «<модель> <запрос>»;
  текст уведомления сразу показывает актуальное слово;
- **диктовка завершается тремя способами**: слово окончания (по умолчанию
  «стоп», настраивается), **пауза 5 секунд** после последней речи — запрос
  уйдёт сам, или пустое слово окончания — отправка по каждой паузе;
  кодовое слово ловится уже на недоговорённой фразе, без ожидания тишины;
  слово окончания тоже проверяется на частичном тексте;
- **символы, которые не озвучивать** (⚙): эти символы вырезаются из
  ответа перед TTS. По умолчанию `*_#~`` ` (markdown), можно дописать
  любые — например эмодзи;
- уведомление ассистента **живое**: показывает актуальное состояние —
  «Слушаю кодовое слово…» → «Записываю…» → «Отправляю запрос…» →
  «Отвечаю голосом…» → снова «Слушаю…»;
- боковое меню (☰) — **список всех ИИ на сервере**: провайдеры с моделями
  и псевдонимы, тап по названию подставляет модель в поле ввода;
- в боковом меню можно **задать свои слова для моделей** («дип сик» →
  deepseek, «фри» → openrouter/free — что угодно): работают и в тексте,
  и голосом; встроенные фразы: «дип сик»/«депп сик»/«деп сик» → deepseek,
  «фри»/«free» → free-модель OpenRouter (алиас бекенда);
- распознавание — **офлайн-движок Vosk** (русская модель vosk-model-small-ru
  вшита в приложение, ~46 МБ): аудио читается напрямую через AudioRecord,
  без системного SpeechRecognizer и его лимита сессий, поэтому ассистент
  не «умирает» после нескольких запросов и одинаково работает в фоне,
  с выключенным экраном и без интернета;
- сервис держит wakelock, чтобы CPU не засыпал, пока ассистент слушает
  с выключенным экраном (на агрессивных прошивках всё равно отключите
  оптимизацию батареи);
- вручную набранные запросы при включённом ассистенте тоже озвучиваются.

В строке быстрых подсказок первыми стоят бесплатные модели:
`gemini`, `фри`, `deepseek`, затем остальные, `claude` — в конце.
Алиасы «фри», «free», «deepseek», «dots» и т.п. резолвятся на бекенде
(см. `LOOK_MODEL_ALIASES`); приложение пересылает слово как есть.

Разрешения запрашиваются при первом включении: микрофон и уведомления.
Для надёжной работы в фоне на MIUI/Huawei и т.п. стоит отключить
оптимизацию батареи для приложения. Распознаванию нужны Google-сервисы
речи (на любом обычном Android они есть); если их нет — ассистент честно
напишет об этом в чат, а текстовый режим продолжит работать.

## Возможности

- чат: история сообщений, индикатор «Думаю…», автопрокрутка; сообщения
  пользователя — справа, ответы — слева;
- руками можно писать просто вопрос без «лук» — модель подставится сама
  (gemini); формат «лук <модель> <запрос>» тоже работает;
- подсказки моделей — подтягиваются с `GET /api/v1/models`: тап по чипу
  подставляет модель в поле ввода;
- адрес сервера и кодовые слова ассистента меняются в приложении
  (шестерёнка в шапке) и сохраняются; уведомление ассистента показывает
  актуальное кодовое слово и обновляется сразу после смены;
- долгое нажатие на сообщение копирует его текст;
- клавиша Enter на клавиатуре = «Отправить».

## Где взять APK

Готовый debug-APK лежит в корне проекта:

```
look-chat-android/LookChat-debug.apk
```

Либо соберите сами: `./gradlew assembleDebug` — результат появится в
`app/build/outputs/apk/debug/app-debug.apk`.

## Как подключиться к бекенду

Адрес по умолчанию выбирается автоматически:

- **Эмулятор:** `http://10.0.2.2:8080` (это ваш компьютер изнутри эмулятора).
- **Реальный телефон:** `http://192.168.0.16:8080` — IP этого компьютера
  вшит в приложение, работает сразу, если телефон в той же Wi-Fi сети
  (WiFI-k3022898). Если роутер выдаст компьютеру другой IP — новый можно
  вписать в настройках приложения (⚙ в шапке), он сохранится.

Быстро перекинуть APK на телефон без Telegram: в папке
`/home/look/lookchat-share` лежит APK, и по Wi-Fi он отдаётся сервером
`http://192.168.0.16:8000/LookChat-debug.apk` — откройте эту ссылку в
браузере телефона и скачайте (сервер запускается командой
`python3 -m http.server 8000 --directory /home/look/lookchat-share`,
остановить: `pkill -f "http.server 8000"`).

Трафик к локальному серверу идёт по обычному `http` — в манифесте
разрешён cleartext (`usesCleartextTraffic="true"`). Распознавание речи
полностью офлайн (Vosk), в интернет ходят только запросы к бекенду.

## Сборка из командной строки

```bash
./gradlew assembleDebug     # debug-APK
./gradlew assembleRelease   # release-APK (без подписи)
```

Нужен JDK 17+ и Android SDK (platform 36, build-tools 36). В Android Studio
достаточно открыть папку проекта — `local.properties` студия создаст сама.

## Структура

```
app/src/main/java/com/look/chat/
  MainActivity.kt          точка входа (ComponentActivity + Compose)
  ChatViewModel.kt         состояние чата, отправка запросов
  ui/
    ChatScreen.kt          весь UI: лента, пузыри, подсказки, настройки
    theme/Theme.kt         тема (динамические цвета Material You)
  data/
    LookApi.kt             OkHttp-клиент бекенда (process + models)
    Dtos.kt                JSON-модели запросов/ответов
    Settings.kt            адрес сервера в SharedPreferences
```

Контракт бекенда: `POST /api/v1/process` c телом
`{"text": "<модель> <запрос>"}` →
`{"status":"ok","model":…,"provider":…,"answer":…,"elapsed_ms":…}`.
При ошибке — `{"status":"error","error":{"code","message"}}`.
Приложение само подставляет модель, если её не назвали, и прогоняет
русские псевдонимы («гемини» → gemini и т.п.).
